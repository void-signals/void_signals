name: void_signals_workspace
description: Workspace for void_signals packages
publish_to: none

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: '>=3.19.0'

dev_dependencies:
  melos: ^6.1.0
  lints: ^4.0.0
  test: ^1.27.0

# Melos configuration
dependency_overrides:
  # Add any workspace-wide dependency overrides here
